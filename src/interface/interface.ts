interface UserType {
    firstName: string,
    lastName: string,
    age: number
}

function grret(user: UserType) { // greet using the UserType

}

let user: UserType = {
    firstName: "Omkar",
    lastName: "Tarle",
    age: 22,
}